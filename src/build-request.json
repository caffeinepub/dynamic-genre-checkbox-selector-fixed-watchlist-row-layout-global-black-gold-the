{
  "kind": "build_request",
  "title": "Fix black screen on page refresh",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Investigate and fix the black screen issue that occurs after page refresh, ensuring the manga list and all UI elements are visible without requiring mouse selection",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ],
        "quotes": [
          "still black screen after refresh"
        ]
      },
      "acceptanceCriteria": [
        "Page loads with visible content after refresh",
        "Background colors render correctly with the warm sepia/ink palette",
        "Text is visible without requiring mouse selection",
        "All manga cards and UI components display properly on initial load"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Verify that the OKLCH color system in index.css and tailwind.config.js is properly applied and not causing rendering issues with black backgrounds",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ]
      },
      "acceptanceCriteria": [
        "OKLCH colors are correctly parsed and applied by browsers",
        "Fallback colors are provided for browsers that don't support OKLCH",
        "Background colors use the intended warm sepia tones, not black"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Check service worker cache behavior to ensure it's not serving stale black screen content after refresh",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-latest"
        ]
      },
      "acceptanceCriteria": [
        "Service worker cache-first strategy does not cause black screen",
        "CSS and static assets are properly cached and served",
        "Cache invalidation works correctly when styles are updated"
      ]
    }
  ],
  "constraints": [
    "Preserve the existing OKLCH warm sepia/ink color palette",
    "Do not modify the backend actor or data models",
    "Maintain compatibility with the service worker caching strategy"
  ],
  "nonGoals": [
    "Changing the overall visual design or color scheme",
    "Modifying manga entry data structures",
    "Implementing new features unrelated to the display issue"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}